<h1>~show~</h1>
<h2><% @user.name %></h2>
<Hr>
<% if @user.present? %>
  <h1><%= @user[:name] %></h1>
  <h2><%= @user[:id] %></h2>
  <p><%= @user[:username] %></p>
  <ul>
      <li>E-mail : <%= @user[:email] %></li>
      <li>About   : <%= @user[:about] %></li>
  </ul>
  <h3>pet_list</h3>
  <% if @user.users_pets.present? %>
    <ul>
      <% @user.users_pets.each do |pet| %>
        <li><%= pet.name %>:<%= pet.type.name %></li>
      <% end %>
    </ul>
  <% else %>
    <p>ペットが登録されていません</p>
  <% end %>
<% else %>
  <p>urlが間違っています</p>
<% end %>
